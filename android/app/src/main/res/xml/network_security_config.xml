<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- RPC daemons are typically plain HTTP; allow cleartext so custom nodes work. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
